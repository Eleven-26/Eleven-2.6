1、目录结构：
Eleven 2.6----------------------框架名称
|
|--application------------------应用目录
| |--admin----------------------模块目录(后台)
| | |--config-------------------后台配置文件
| | |--controller---------------后台控制器
| | |--core---------------------后台公共基础类
| | |--views--------------------视图
| |--api------------------------接口目录
| | |--mobile-------------------APP接口目录
| |--config---------------------模块配置目录
| |--home-----------------------模块目录(前台)
|
|--common-----------------------公共文件夹
| |--config---------------------配置文件
| |--helper---------------------自定义扩展类库、辅助函数目录
| |--library--------------------自定义、第三方基础类库
| |--models---------------------模型
|
|--eleven-----------------------框架核心目录
| |--fujilanx-------------------框架核心类库目录
| | |--cache--------------------缓存基础类库
| | |--database-----------------数据库基础类库
| | |--log----------------------日志基础类库
| | |--queue--------------------队列基础类库
| |--lang-----------------------多语言目录
|
|--public-----------------------网站对外访问目录
| |--upload---------------------下载文件存在目录
| |--static---------------------静态文件存放目录
| | |--css----------------------CSS目录
| | |--js-----------------------JS目录
| | |--html---------------------HTML缓存目录
| |--index.php------------------框架入口文件
|
|--runtime----------------------临时目录
| |--cache----------------------缓存目录
| |--log------------------------日志
|
|--vendor-----------------------第三方类库目录
| |--composer-------------------composer目录

2、命名空间，须符合PSR-4规范：
|--App--------------------------顶级命名空间
| |--eleven---------------------核心目录命令空间
| |--eleven---------------------核心目录命令空间
| |--eleven---------------------核心目录命令空间

3、命名规范：
### 函数和类、属性命名
*   类的命名采用驼峰法，并且首字母大写，例如 `User`、`UserType`，控制器默认不需要添加后缀，
*   例如`UserController`应该直接命名为`User`；模型加上后缀区分，例如`UserModel`
*   函数的命名使用小写字母和下划线（小写字母开头）的方式，例如 `get_client_ip`；
*   方法的命名使用驼峰法，并且首字母小写，例如 `getUserName`；
*   属性的命名使用驼峰法，并且首字母小写，例如 `tableName`、`instance`；
*   以双下划线“__”打头的函数或方法作为魔法方法，例如 `__call` 和 `__autoload`；

### 常量和配置
*   常量以大写字母和下划线命名，例如 `APP_PATH`和 `ELEVEN_PATH`；
*   配置参数以小写字母和下划线命名，例如 `url_route_on` 和`url_convert`；

### 数据表和字段
*   数据表和字段采用小写加下划线方式命名，并注意字段名不要以下划线开头，例如 `think_user` 表和 `user_name`字段
*   不建议使用驼峰和中文作为数据表字段命名。

4、控制器、模型方法代码规范：
类方法前面务必写上指定备注(作用、作者、日期)，否则报错(初始化时用反射API检测，保证注释规范)，格式如下
/**
 * Note：用户登录
 * User：Eleven
 * Date：2018/11/26 11:11:11
 */

5、URL格式:host/模块/控制器/操作?参数1=值1&参数2=值2
http://www.eleven.com/admin/index/index/id/1126

